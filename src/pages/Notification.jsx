// 3ファイルとも、中身はこれでOK（クラス名だけ変えてください）
import React from "react";
import Layout from "../components/common/Layout";

const Notification = () => {
  return <Layout headerContent="通知" mainContent={<div>作成中</div>} />;
};
export default Notification;